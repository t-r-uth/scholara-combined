import SignInClient from './SignInClient'

export const metadata = {
  title: 'Sign in — Scholara',
  description: 'Sign in to Scholara',
}

export default function SignInPage() {
  return <SignInClient />
}
